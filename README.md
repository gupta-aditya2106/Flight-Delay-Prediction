# Flight-Delay-Prediction
 Processed 3M+ flight records via EDA, IQR-based outlier removal, Label/one-hot encoding and MinMax scaling across flight data signals.  Modeled delays using XGBoost, Neural Networks &amp;  LSTM (9.53 MAE); built a genetic algo optimizer via selection, crossover &amp;  mutation
